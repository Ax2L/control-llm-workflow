
def collect_tasks(input_data):
    # Collect tasks from Jira
    tasks = input_data
    print("thank you :D")
    #save_new_tasks("data/tasks.json", tasks)
    return tasks


def check_prompts(input_data):
    # Check prompts from Jira
    prompts = input_data
    print(f"Here are the prompts: {prompts}")
    #save_new_tasks("data/tasks.json", tasks)
    return prompts